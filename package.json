{
  "name": "chefdefensor",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.140.0"
  },
  "devDependencies": {
    "vite": "^2.0.0",
    "gh-pages": "^3.2.3"
  }
}